export interface MonthRow {
  month: string; // e.g. "January"
  revenue: number;
  profit: number;
}

export type BranchRow = {
  branchName: string;
  revenue: number;
  profit: number;
};
