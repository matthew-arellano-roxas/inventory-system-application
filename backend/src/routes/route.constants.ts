export enum ROUTE {
  PRODUCT = '/api/product',
  BRANCH = '/api/branch',
  CATEGORY = '/api/category',
  TRANSACTION = '/api/transaction',
  STOCK = '/api/stock',
  REPORT = '/api/report',
}

export const TTL = 60;
